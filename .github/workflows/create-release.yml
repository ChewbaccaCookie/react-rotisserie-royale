name: Deploy Server
on:
    push:
        branches:
            - master

jobs:
    create-release:
        name: "Create new release and deploy server"
        if: "! contains(toJSON(github.event.commits.*.message), '[skip-ci]')"
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
              with:
                  persist-credentials: false
            - uses: actions/setup-node@v1
              with:
                  node-version: 13
                  registry-url: https://npm.onedash.dev/

            - uses: actions/cache@v1
              with:
                  path: ~/.pnpm-store
                  key: ${{ runner.os }}-node-${{ hashFiles('**/pnpm-lock.yaml') }}
                  restore-keys: |
                      ${{ runner.os }}-node-
            - name: 🏗 Install dependencies
              run: npx pnpm install
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

            - name: Semantic Release
              env:
                  GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
                  NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
              run: npx semantic-release

            - name: "🚦 Build server"
              run: npm run build

            - name: 🚀 Deploy to ftp
              uses: SamKirkland/FTP-Deploy-Action@3.1.0
              with:
                  ftp-server: ${{ secrets.FTP_SERVER }}
                  ftp-username: ${{ secrets.FTP_USERNAME }}
                  ftp-password: ${{ secrets.FTP_PASSWORD }}
                  local-dir: build/
                  known-hosts: ssh-rsa AAAAB3NzaC1yc2EAAAABIwAAAIEA2zJZx6VkSJC14SILqvzLiR3v8eS0D7Zs7C7771yDwa90UW8JkhK23dk/G8R74kdcWYEOn3kCSr/2rz+UtYThZeWUa5F6BcT/X5Gwh1RAlw5ESxnRr+hiZhYLI7BivVIo5EzecIs0KEojMRUkXI+ClHIcyXBY6grflQFgjZphFMs=
